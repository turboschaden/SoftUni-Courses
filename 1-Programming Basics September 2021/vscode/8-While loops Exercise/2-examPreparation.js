function deba(input) {
  let poorGradesLimit = Number(input[0]);
  let poorGradesCount = 0;
  let index = 1;
  let isEnough = false;
  let taskName =''
  let currentTask = input[index];
  while (poorGradesCount < poorGradesLimit) {
    if (poorGradesCount === poorGradesLimit) {
   currentTask = input[]
    }
  }
}
